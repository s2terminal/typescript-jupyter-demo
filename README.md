## Jupyter+Deno
### [mise](https://mise.jdx.dev/)の場合

```
$ mise trust --all
$ mise install
$ mise exec -- deno jupyter --install
$ mise exec -- uv run jupyter lab
```

localhost:8888

### Dockerの場合

```
$ docker compose up
```
